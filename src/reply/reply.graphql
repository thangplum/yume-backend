type Reply {
  id: ID!
  created: String!
  comment: String!
  comments: [Comment!]
}

type Query {
  reply(id: ID!): Reply
}

type Mutation {
  createReply(post: ID!, comment: String!): Reply
  deleteReply(id: ID!): Reply
}
